{
  "private": true,
  "devDependencies": {
    "husky": "^9.1.7",
    "prettier": "^3.4.2"
  },
  "scripts": {
    "prepare": "husky",
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "verify": "cd client && npm run build && npm run lint && cd ../server && npm run build && npm run lint && cd .. && npm run format"
  }
}
